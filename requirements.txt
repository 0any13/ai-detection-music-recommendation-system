# ============================================================
# Music Recommendation System - Full Requirements
# Install: pip install -r requirements.txt
# or with conda env active: conda run -n musicrec pip install -r requirements.txt
# ============================================================

# --- HTTP requests (used in every API client) ---
requests>=2.31.0

# --- Environment variables (.env file loading) ---
python-dotenv>=1.0.0

# --- Core data processing ---
pandas>=2.0.0
numpy>=1.24.0

# --- Collaborative filtering (ALS / matrix factorization) ---
implicit>=0.7.0
scipy>=1.10.0

# --- Machine learning (cosine similarity, etc.) ---
scikit-learn>=1.3.0

# --- Progress bars ---
tqdm>=4.65.0

# --- Plotting (optional, for analysis scripts) ---
matplotlib>=3.7.0
seaborn>=0.12.0

# --- Thread control (implicit dependency of implicit library) ---
threadpoolctl>=3.1.0

# --- Web framework (if you add a Flask UI later) ---
flask>=2.3.0

# --- MusicBrainz MBID lookup (get_mbids.py) ---
musicbrainzngs>=0.7.1

# ============================================================
# NOT in pip - install separately if needed:
#   conda install -c conda-forge implicit   (if pip build fails)
# ============================================================